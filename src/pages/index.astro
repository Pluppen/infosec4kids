---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
	<head>
        <BaseHead title={"InfoSäk för barn / InfoSec For Kids"} description={"IT-Säkerhet beskrivet genom olika barnberättelser för att barn ska få en förståelse men även ibland då det är lättare som vuxen på det sättet."} />
        <meta http-equiv="refresh" content="0;url=/sv/" />
	</head>
	<body class="kids-theme">
		<Footer />
	</body>
</html>
